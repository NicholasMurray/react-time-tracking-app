{
  "name": "react-timers",
  "version": "0.1.0",
  "private": true,
  "proxy": "http://localhost:3001/",
  "devDependencies": {
    "concurrently": "^3.1.0",
    "react-scripts": "0.8.4"
  },
  "dependencies": {
    "body-parser": "^1.15.2",
    "react": "^15.4.1",
    "react-dom": "^15.4.1",
    "uuid": "^3.0.1"
  },
  "scripts": {
    "concurrently-start": "concurrently \"npm run start\" \"npm run server\"",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "server": "babel-node server.js"
  }
}
